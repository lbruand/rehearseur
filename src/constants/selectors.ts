/**
 * DOM selectors used to query rrweb player elements.
 * Consolidates selector strings for better maintainability and type safety.
 */

export const DOM_SELECTORS = {
  RR_PROGRESS: '.rr-progress',
  RR_CONTROLLER_BTN: '.rr-controller__btns button',
} as const;